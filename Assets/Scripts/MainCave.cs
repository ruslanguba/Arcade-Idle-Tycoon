using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainCave : MonoBehaviour
{
    [SerializeField] ItemDeposit ItemDeposit;
    [SerializeField] Dictionary<ItemTypes, int> currentResources;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
